import { assert } from 'chai';
import app from './index';

describe('Index', () => {
  it('should be true', () => {
    assert.equal(true, true);
  });

  it('should be false', () => {
    assert.equal(false, false);
  });
});
