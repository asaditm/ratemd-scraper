import { assert } from 'chai';

describe('Index', () => {
  it('should be true', () => {
    const index = true;
    assert.equal(index, true);
  });

  it('should be false', () => {
    assert.equal(false, false);
  });
});
