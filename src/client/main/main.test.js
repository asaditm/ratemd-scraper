import { assert } from 'chai';

describe('Main Module', () => {
  it('should be true', () => {
    assert.equal(true, true);
  });

  it('should be false', () => {
    assert.equal(false, false);
  });
});