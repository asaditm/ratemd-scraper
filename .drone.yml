cache:
  mount:
    - node_modules
build:
  image: node:wheezy
  commands:
    - npm install --unsafe-perm
    - npm run build

deploy:
  ssh:
    host: myDomain.ca
    user: drone
    commands:
      - docker-restart awesomeApp-staging
    when:
      branch: develop
  ssh:
    host: myDomain.ca
    user: drone
    commands:
      - docker-restart awesomeApp
    when:
      branch: master
